var INFINITY = '+Inf';
export var ImageLoadTrackerBuckets = [4000, 2000, 1000, 500, 100, 50];
export var RenderTrackerBuckets = [4000, 2000, 1000, 500]; // Group load times in to buckets for metric cardinality

export var getMetricBucket = function getMetricBucket(metricValue, buckets) {
  return metricValue >= 0 ? buckets.reduce(function (lowestBucket, currentBucket) {
    return metricValue <= currentBucket ? currentBucket.toString() : lowestBucket.toString();
  }, INFINITY) : null;
};